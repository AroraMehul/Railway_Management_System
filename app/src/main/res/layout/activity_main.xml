<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/grad_bg"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:background="@drawable/et_bg"
        android:layout_centerInParent="true">
    <Button
        android:layout_margin="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/checkTrain"
        android:background="@drawable/btn_bg"
        android:text="Check Trains"
        android:textColor="@color/white"
        />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/checkTrain"
            android:id="@+id/bookings"
            android:layout_margin="20dp"
            android:background="@drawable/btn_bg"
            android:text="Bookings"
            android:textColor="@color/white"
            />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:layout_below="@id/bookings"
            android:id="@+id/newBooking"
            android:background="@drawable/btn_bg"
            android:text="New Booking"
            android:textColor="@color/white"
            />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:layout_below="@id/newBooking"
            android:id="@+id/checkStation"
            android:background="@drawable/btn_bg"
            android:text="Check Stations"
            android:textColor="@color/white"
            />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:layout_below="@id/checkStation"
            android:id="@+id/checkSeats"
            android:background="@drawable/btn_bg"
            android:text="Check Seats"
            android:textColor="@color/white"
            />



    </RelativeLayout>

    <android.support.design.widget.FloatingActionButton
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="20dp"
        android:layout_marginRight="20dp"
        android:id="@+id/user"
        android:onClick="changeUser"
        android:background="@color/transparent"
        android:layout_marginTop="20dp"
        android:layout_alignParentRight="true"
        android:src="@drawable/ic_person_outline_black_24dp"
        />


</RelativeLayout>